Adapt a pretrained Qwen3 BASE text checkpoint (Qwen3-4B/8B-Base — since Qwen3.6 ships only as 27B+ instruct/multimodal, no base) into a text DIFFUSION language model: a block-discrete masked/absorbing-state diffusion LM (BD3-LM/LLaDA/Dream-style), built fresh in /mnt/minio/shiyil/qwen3, with Qwen3.6 as the north-star to scale to once the recipe works. The three pillars: (1) attention surgery causal->block-causal, (2) the masked-diffusion objective with in-place (NOT next-token-shifted) loss weighted by 1/t, (3) AR->diffusion weight adaptation that preserves the base model's knowledge. Bar for "done": coherent free-running text from iterative unmasking that approaches the AR base's quality on a held-out benchmark subset (generative PPL + a few zero-shot tasks) at matched compute.
